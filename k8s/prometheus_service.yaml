apiVersion: v1
kind: Service
metadata:
  name: prometheus-service
spec:
  selector:
    app: prometheus
  ports:
  - protocol: TCP
    port: 9090  # Порт, на котором доступен Prometheus
    targetPort: 9090
  type: ClusterIP  # Сервис доступен внутри кластера Minikube
